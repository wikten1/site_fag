# Values must be supplied by FAG. Never place credentials in browser assets.
HOST=127.0.0.1
PORT=3000
PUBLIC_ORIGIN=http://127.0.0.1:3000
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# One institutional address each; MAIL_FROM must be authorized by the provider.
MAIL_FROM=
CONTACT_TO=
