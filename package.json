{
  "name": "fag-institucional",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build-site.js",
    "build:check": "node scripts/build-site.js --check",
    "start": "node --env-file-if-exists=.env server/contact-server.js",
    "test": "node tests/news-model.test.js && node --test tests/contact-server.test.js tests/site.test.js tests/opportunities.test.js",
    "check:mail": "node --env-file-if-exists=.env server/contact-server.js --verify-mail"
  },
  "dependencies": {
    "nodemailer": "10.0.8"
  }
}
